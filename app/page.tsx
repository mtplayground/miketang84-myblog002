export default function HomePage() {
  return (
    <main className="page-shell">
      <section className="hero-card">
        <p className="eyebrow">Next.js 15 + TypeScript</p>
        <h1>myblog002</h1>
        <p className="lede">
          The project bootstrap is in place. Subsequent issues will add the
          admin workflow, PostgreSQL-backed content model, and public blog
          features.
        </p>
      </section>
    </main>
  );
}
